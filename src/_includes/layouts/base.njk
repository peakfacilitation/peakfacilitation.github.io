<!DOCTYPE html>
<html lang="en">

<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="shortcut icon" href="/images/favico.png">
<link rel="apple-touch-icon" href="/images/favico.png">
<title>Peak Facilitation Group &gt; {{ title }}</title>
<link rel="stylesheet" href="css/peak.css">
<link rel="stylesheet" href="css/styles.css">
<!-- Global site tag (gtag.js) - Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id={{ site.googleAnalyticsId }}"></script>
<script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', '{{ site.googleAnalyticsId }}');
</script>
</head>

<body>
<div id="main-container">

	<div id="header">
    	<h1 id="branding"><a href="index.html">Peak Facilitation Group</a></h1>
    	<ul id="main-menu">
{%- for item in navigation.main %}
        	<li class="menu-item{% if item.class %} {{ item.class }}{% endif %}{% if menuParent == item.label %} open{% endif %}"><a href="{{ item.url }}">{{ item.label }}</a>
{%- if item.children %}
            	<ul class="sub-menu">
{%- for child in item.children %}
                	<li class="sub-item{% if menuItem == child.label %} selected{% endif %}">{% if child.url %}<a href="{{ child.url }}">{{ child.label | safe }}</a>{% else %}{{ child.label }}{% endif %}</li>
{%- endfor %}
                </ul>
                </li>
{%- else %}
</li>
{%- endif %}
{%- endfor %}
            </li>
        </ul>
    </div>

{% if title != "Home" %}
    <div id="page-title-bar">
    	<h2>{{ title }}</h2>
    </div>
{% endif %}

{% if forward %}
    <div id="forward-container">
    	<div id="forward-text">
        {{ forward.text | safe }}
        </div>
        <div id="forward-image">
        	<img src="{{ forward.image }}" width="{{ forward.width | default(486) }}" height="{{ forward.height | default(266) }}" alt="{{ forward.alt }}" loading="lazy">
        </div>
    </div>
{% endif %}

{% if stripe %}
    <div id="stripe-container">
    	<p>{{ stripe | safe }}</p>
    </div>
{% endif %}

    <div id="content-container">
{{ content | safe }}
    </div>

    <div id="footer">
    	<p>&copy;{{ site.copyright }}</p>
    </div>

</div>
</body>
</html>
