<!DOCTYPE html>
<html lang="en">

<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="shortcut icon" href="/images/favico.png">
<link rel="apple-touch-icon" href="/images/favico.png">
<title>Peak Facilitation Group &gt; {{ title }}</title>
<link rel="stylesheet" href="css/peak.css">
<link rel="stylesheet" href="css/styles.css">
<!-- Global site tag (gtag.js) - Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id={{ site.googleAnalyticsId }}"></script>
<script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', '{{ site.googleAnalyticsId }}');
</script>
</head>

<body>
<div id="main-container">

	<div id="header" class="relative">
    	<h1 id="branding"><a href="index.html">Peak Facilitation Group</a></h1>
    	<nav id="main-menu" class="absolute right-10 top-5 flex gap-8 list-none m-0 p-0">
{%- for item in navigation.main %}
        	<li class="group relative{% if item.class %} {{ item.class }}{% endif %}{% if menuParent == item.label %} is-active{% endif %}">
				<a href="{{ item.url }}" class="block py-2 text-lg uppercase text-peak-blue-nav hover:text-peak-purple transition-colors duration-200 no-underline {% if menuParent == item.label %}text-peak-purple border-b-2 border-peak-purple{% endif %}">{{ item.label }}</a>
{%- if item.children %}
            	<ul class="sub-menu absolute top-full {% if item.class == 'contact' %}right-0{% else %}left-0{% endif %} min-w-48 py-3 m-0 list-none bg-white rounded-md shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50 overflow-hidden">
{%- for child in item.children %}
                	<li class="m-0 p-0">{% if child.url %}<a href="{{ child.url }}" class="block px-6 py-2.5 text-[15px] leading-normal whitespace-nowrap text-peak-purple-light hover:text-peak-purple hover:bg-peak-gray-light no-underline transition-colors duration-150 {% if menuItem == child.label %}text-peak-purple font-semibold{% endif %}">{{ child.label | safe }}</a>{% else %}<span class="block px-6 py-2.5 text-[15px] leading-normal whitespace-nowrap text-peak-purple-light">{{ child.label }}</span>{% endif %}</li>
{%- endfor %}
                </ul>
{%- endif %}
			</li>
{%- endfor %}
        </nav>
    </div>

{% if title != "Home" %}
    <div id="page-title-bar">
    	<h2>{{ title }}</h2>
    </div>
{% endif %}

{% if forward %}
    <div id="forward-container">
    	<div id="forward-text">
        {{ forward.text | safe }}
        </div>
        <div id="forward-image">
        	<img src="{{ forward.image }}" width="{{ forward.width | default(486) }}" height="{{ forward.height | default(266) }}" alt="{{ forward.alt }}" loading="lazy">
        </div>
    </div>
{% endif %}

{% if stripe %}
    <div id="stripe-container">
    	<p>{{ stripe | safe }}</p>
    </div>
{% endif %}

    <div id="content-container">
{{ content | safe }}
    </div>

    <div id="footer">
    	<p>&copy;{{ site.copyright }}</p>
    </div>

</div>
</body>
</html>
